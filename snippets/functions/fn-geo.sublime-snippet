<snippet>
    <content><![CDATA[
import { geolocation } from '@vercel/functions';

export function GET(request: Request) {
  const { city, country, region, latitude, longitude } = geolocation(request);
  ${0}
  return Response.json({ city, country, region, latitude, longitude });
}
]]></content>
    <tabTrigger>vc:fn:geo</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>Geolocation from request</description>
</snippet>
