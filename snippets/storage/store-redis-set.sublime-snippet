<snippet>
    <content><![CDATA[
import { Redis } from '@upstash/redis';

const redis = new Redis({
  url: process.env.KV_REST_API_URL!,
  token: process.env.KV_REST_API_TOKEN!,
});

await redis.set('${1:key}', ${2:value}, { ex: ${3:3600} });
${0}
]]></content>
    <tabTrigger>vc:store:redis:set</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>Redis set value with TTL</description>
</snippet>
