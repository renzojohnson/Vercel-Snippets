<snippet>
    <content><![CDATA[
import { upload } from '@vercel/blob/client';

const blob = await upload(${1:file}.name, ${1:file}, {
  access: 'public',
  handleUploadUrl: '${2:/api/upload}',
});
${0}
]]></content>
    <tabTrigger>vc:store:blob:client</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>Vercel Blob client-side upload</description>
</snippet>
