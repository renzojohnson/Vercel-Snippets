<snippet>
    <content><![CDATA[
import { unstable_cache } from 'next/cache';

const getCachedData = unstable_cache(
  async (${1:id}: string) => {
    ${0:// Expensive operation}
    return { id: ${1:id} };
  },
  ['${2:cache-key}'],
  { revalidate: ${3:3600}, tags: ['${4:data}'] }
);
]]></content>
    <tabTrigger>vc:env:unstable:cache</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>unstable_cache for data caching</description>
</snippet>
