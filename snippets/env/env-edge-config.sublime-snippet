<snippet>
    <content><![CDATA[
import { createClient } from '@vercel/edge-config';

const edgeConfig = createClient(process.env.EDGE_CONFIG);
const ${1:value} = await edgeConfig.get<${2:string}>('${1:value}');
${0}
]]></content>
    <tabTrigger>vc:env:edge:config</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>Edge Config client init</description>
</snippet>
