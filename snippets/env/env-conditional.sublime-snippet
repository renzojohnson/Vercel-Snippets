<snippet>
    <content><![CDATA[
const config = {
  apiUrl: process.env.VERCEL_ENV === 'production'
    ? '${1:https://api.example.com}'
    : '${2:http://localhost:3001}',
  debug: process.env.VERCEL_ENV !== 'production',
};
${0}
]]></content>
    <tabTrigger>vc:env:conditional</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>Environment-conditional config</description>
</snippet>
