<snippet>
    <content><![CDATA[
const bypassToken = request.headers.get('x-vercel-protection-bypass');
if (bypassToken !== process.env.VERCEL_AUTOMATION_BYPASS_SECRET) {
  return new Response('Unauthorized', { status: 401 });
}
${0}
]]></content>
    <tabTrigger>vc:deploy:protection</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>Deployment protection bypass</description>
</snippet>
