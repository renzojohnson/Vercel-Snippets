<snippet>
    <content><![CDATA[
import { generateText, tool } from 'ai';
import { openai } from '@ai-sdk/openai';
import { z } from 'zod';

const result = await generateText({
  model: openai('${1:gpt-4o}'),
  tools: {
    ${2:weather}: tool({
      description: '${3:Get the weather for a location}',
      parameters: z.object({
        ${4:location}: z.string().describe('${5:City name}'),
      }),
      execute: async ({ ${4:location} }) => {
        ${0:return { temperature: 72, condition: 'sunny' };}
      },
    }),
  },
  prompt: '${6:What is the weather in San Francisco?}',
});
]]></content>
    <tabTrigger>vc:ai:tool</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>AI tool calling</description>
</snippet>
