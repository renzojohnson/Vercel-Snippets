<snippet>
    <content><![CDATA[
import { embed } from 'ai';
import { openai } from '@ai-sdk/openai';

const { embedding } = await embed({
  model: openai.embedding('${1:text-embedding-3-small}'),
  value: '${2:Text to embed}',
});
${0}
]]></content>
    <tabTrigger>vc:ai:embed</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>AI embeddings</description>
</snippet>
