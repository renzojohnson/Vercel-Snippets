<snippet>
    <content><![CDATA[
export const revalidate = ${1:60};

export default async function ${2:Page}() {
  const data = await fetch('${3:https://api.example.com/data}').then(res => res.json());

  return (
    <div>
      ${0}
    </div>
  );
}
]]></content>
    <tabTrigger>vc:next:isr</tabTrigger>
    <scope>source.js, source.ts, source.tsx</scope>
    <description>ISR with revalidate (route segment config)</description>
</snippet>
